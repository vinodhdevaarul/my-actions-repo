name: Expressions & Contexts
on: [push, issues]
jobs:
  runs-on-all-events:
    runs-on: ubuntu-latest
    name: Runs all Events
    steps:
    - name: echo contexts
      run: |
        echo ${{ toJson(github) }}
        echo ${{ toJson(runner) }}
        echo ${{ toJson(job) }}
        echo ${{ toJson(steps) }}